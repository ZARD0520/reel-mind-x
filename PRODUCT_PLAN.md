# ReelMind 产品规划：AI 原生无限画布与视频生产工作流

> 文档版本：v1.0  
> 制定日期：2026-08-01  
> 规划周期：未来 12 个月  
> 产品阶段：从 AI 视频剪辑器升级为 AI 原生视频创作工作台

---

## 1. 文档目的

本文用于统一 ReelMind 后续产品方向、功能边界和建设顺序，重点回答以下问题：

1. 为什么 ReelMind 需要无限画布；
2. 无限画布、AI Agent、分镜工作台和现有时间轴如何协同；
3. 从脚本到成片的核心用户流程是什么；
4. 哪些能力优先建设，哪些能力暂缓；
5. 每个阶段用什么指标判断产品是否成立。

本文是产品规划，不代替具体版本的 PRD、交互稿和技术设计。每个里程碑进入开发前，仍需拆出独立 PRD 和技术方案。

---

## 2. 战略结论

### 2.1 产品定位

ReelMind 的目标定位是：

> 面向电商商家、投流团队和短视频工作室的 AI 原生视频创作工作台。用户可以在无限画布中完成 Brief、脚本、分镜脚本、分镜图、分镜视频和声音的连续生产，并在专业时间轴中完成精剪、合成与交付。

一句话价值主张：

> 从一个想法或一份商品 Brief，生成一条真正可调整、可追溯、可批量复用的视频成片。

### 2.2 核心产品结构

产品采用“双工作区”结构：

```text
无限画布 Creative Canvas
负责：构思、资料、脚本、分镜、生成、版本、关系、协作
                  ↓ 编译为成片
专业时间轴 Timeline Editor
负责：帧级精剪、叠加、字幕、音频、转场、预览、渲染、导出
```

无限画布是视频生产的主入口，不是附属功能；时间轴是最终精修和交付工作台，不被画布替代。

### 2.3 产品壁垒

无限画布本身不是壁垒。真正需要构建的壁垒是：

1. **上下文连续性**：商品、角色、风格、品牌规范和创作意图能贯穿所有节点；
2. **结构化视频生产**：脚本、镜头、视觉、声音和时间轴之间存在稳定的数据关系；
3. **可编辑与可追溯**：每个生成结果都能找到输入、模型、参数和上游版本；
4. **从画布到时间轴的可靠编译**：AI 生成不是终点，结果必须能进入专业编辑和导出闭环；
5. **批量广告变体**：围绕 Hook、卖点、CTA、比例、平台和受众快速派生多个成片版本。

---

## 3. 市场参考与借鉴原则

### 3.1 TapNow 的启发

TapNow 当前以 Creative OS 组织产品：Agent 负责理解目标和调度任务，Apps 执行具体能力，画布保存资料、过程和交付物。其关键价值不是单次生图或生视频，而是让真实 Brief、参考素材、生成过程和结果停留在同一个创作空间中。

值得借鉴：

- Agent 可以理解画布上下文并操作具体对象；
- 从 Brief、脚本、分镜到成片形成连续工作流；
- 自动执行与人工确认并存；
- 模板可以复制完整生产流程；
- 生成过程、分支版本和最终结果可追溯；
- 社区作品可以展示完整创作过程并被复用。

不直接照搬：

- 不以模型数量作为主要卖点；
- 不在早期扩张到市场洞察、广告投放和站点运营；
- 不先建设复杂 App 生态；
- 不让节点连接和参数配置成为普通用户的使用负担。

### 3.2 同类产品带来的判断

Freepik Spaces、CapCut 等产品也已经进入无限画布和 AI 工作流方向。因此 ReelMind 不能只做一个通用节点画布，而应聚焦视频创作的结构化链路，并发挥现有时间轴与服务端渲染能力。

### 3.3 参考资料

- [TapNow Creative OS](https://docs.tapnow.ai/zh/docs/start/what-is-tapnow-creative-os)
- [TapNow Agent 生成模式](https://docs.tapnow.ai/zh/docs/agent/choose-a-generation-mode)
- [TapNow 视频拉片与分镜](https://docs.tapnow.ai/en/docs/video-creation/create-storyboard)
- [Freepik Spaces](https://ru.freepik.com/ai/docs/introduction-to-spaces)
- [CapCut AI Video Maker](https://www.capcut.com/tools/ai-video-maker-ua)

---

## 4. 目标用户与首要场景

### 4.1 第一目标用户

#### A. 中小电商商家和品牌运营

- 已有商品图、商品信息和卖点；
- 缺少脚本、分镜与视频制作能力；
- 需要持续制作抖音、小红书、视频号或 TikTok 广告；
- 关注出片速度、商品还原度和成本。

#### B. 广告投流和内容运营人员

- 需要频繁测试不同 Hook、卖点和 CTA；
- 同一个商品需要多个时长、比例和平台版本；
- 关心批量变体和迭代效率，不追求复杂影视制作。

#### C. 小型 AI 视频工作室

- 同时服务多个客户；
- 需要保存品牌资产、参考素材和工作流模板；
- 需要让策划、设计、剪辑在同一项目中协作和审批。

### 4.2 首要产品场景

首个闭环场景：**15～45 秒电商商品广告视频**。

典型任务：

> 上传商品图、Logo、卖点和一条参考视频，生成 3 个创意方向；选择其中一个后生成脚本、分镜图和分镜视频，确认后自动进入时间轴，补齐配音、字幕和音乐，最终导出 9:16 广告成片，并派生两个不同 Hook 的版本。

### 4.3 后续场景

在商品广告闭环成立后，再依次扩展：

1. 品牌宣传片；
2. 口播与知识类短视频；
3. 剧情短片和动画；
4. MV、概念短片和长叙事内容；
5. 团队级批量内容生产。

---

## 5. 信息架构

### 5.1 一级导航

建议产品一级导航为：

| 页面 | 作用 |
|---|---|
| 首页 | 新建项目、最近项目、模板入口、学习案例 |
| 项目空间 | 项目 Brief、画布、时间轴、资产、版本与导出统一入口 |
| 素材库 | 用户级商品、角色、品牌、图片、视频、音频资产 |
| 模板 | 官方工作流、个人模板、团队模板 |
| 任务中心 | 查看生成、渲染、失败重试和消耗 |
| 团队空间 | 成员、共享项目、审批、额度和权限；后期开放 |

### 5.2 项目内工作区

每个项目包含以下视图：

1. **画布**：默认主视图，承载创作流程；
2. **时间轴**：成片精修和导出；
3. **资产**：查看项目使用的全部输入与生成结果；
4. **版本**：查看画布快照、成片版本和分支；
5. **交付**：管理渲染结果、平台规格和下载文件。

画布和时间轴应允许一键切换，并保留各自的视口、选中对象和撤销历史。

---

## 6. 无限画布产品设计

### 6.1 画布职责

无限画布用于表达创作对象之间的关系和演进过程：

```text
Brief / 商品资料 / 品牌规范 / 参考视频
                    ↓
                 创意策略
                    ↓
                  脚本
                    ↓
                 分镜脚本
              ↙      ↓      ↘
          分镜图 A  分镜图 B  分镜图 C
              ↓       ↓       ↓
          分镜视频 A 分镜视频 B 分镜视频 C
              ↘       ↓       ↙
            配音 / 音乐 / 字幕
                    ↓
              时间轴成片版本
```

画布既要支持 Agent 自动搭建，也要允许用户手动增加、连接、替换、分组和调整节点。

### 6.2 页面布局

建议采用以下布局：

```text
┌────────────────────────────────────────────────────────────┐
│ 项目名 / 画布-时间轴切换 / 撤销重做 / 分享 / 点数 / 导出   │
├──────────┬─────────────────────────────────┬───────────────┤
│ 节点与   │                                 │ 属性 / 版本 / │
│ 项目资产 │          无限画布               │ Agent 面板    │
│ 左侧栏   │                                 │ 右侧栏        │
├──────────┴─────────────────────────────────┴───────────────┤
│ 任务栏：生成队列、进度、失败、预计消耗、并发状态          │
└────────────────────────────────────────────────────────────┘
```

核心交互：

- 滚轮缩放、空格拖动画布、框选、多选、复制、对齐和分组；
- 从左栏拖入节点或素材；
- 从节点端口拖线建立上下游关系；
- 双击空白区域快速搜索节点；
- 选中节点后在右侧编辑属性；
- 通过 `@` 在 Agent 对话中引用节点；
- 通过 `/` 快速调用生成或处理能力；
- 支持缩略地图、回到内容、适应视图；
- 支持节点折叠，避免大型项目失控。

### 6.3 节点分类

#### 6.3.1 输入与资料节点

| 节点 | 核心内容 |
|---|---|
| Brief | 目标、受众、平台、时长、卖点、CTA、限制条件 |
| 文档 | PDF、Word、文本、会议纪要等 |
| 商品 | 商品图、名称、卖点、规格、Logo 保留规则 |
| 品牌包 | Logo、色彩、字体、语气、禁用项 |
| 角色 | 人物参考、多角度图、声音与角色设定 |
| 图片 | 本地上传或已有素材 |
| 视频 | 本地视频、参考广告、生成视频 |
| 音频 | 配音、音乐、音效 |
| 参考集合 | 为多个参考资产标注商品、人物、构图、运镜或风格用途 |

#### 6.3.2 策划与结构节点

| 节点 | 核心内容 |
|---|---|
| 创意策略 | 受众洞察、核心概念、情绪、节奏和差异化方向 |
| 脚本 | 标题、Hook、正文、CTA、旁白和台词 |
| 分镜脚本 | 镜头列表、景别、运镜、动作、台词、时长和衔接 |
| 镜头 | 单个 Shot 的完整结构化定义 |
| 提示词 | 独立可复用的模型提示词和负面提示词 |

脚本和分镜脚本节点不是不可编辑的聊天结果，而是结构化文档卡片。用户可以在节点内部编辑段落、镜头和顺序。

#### 6.3.3 生成节点

| 节点 | 输入 | 输出 |
|---|---|---|
| 文本生成 | Brief、文档、文本 | 策略、脚本、分镜脚本、提示词 |
| 图片生成 | 文本、商品、角色、参考图 | 单张或多张图片 |
| 视频生成 | 文本、首帧、尾帧、参考视频 | 视频片段 |
| 配音生成 | 台词、角色声音 | 音频 |
| 音乐生成 | 情绪、风格、时长、参考 | 音乐 |
| 音效生成 | 场景、动作、时长 | 音效 |
| 字幕生成 | 视频或音频 | 带时间码字幕 |

#### 6.3.4 编辑与分析节点

- 视频拉片：拆解镜头、景别、运镜、节奏、字幕和音频；
- 图片局部编辑、扩图、抠图、放大和修复；
- 视频延长、局部修改、插帧、去背景和增强；
- 音频降噪、分离、人声处理；
- 内容审核、品牌一致性检查、商品还原检查；
- 多结果比较和人工选择。

#### 6.3.5 组装与交付节点

| 节点 | 作用 |
|---|---|
| Sequence | 按镜头顺序组织选定的视频、声音和字幕 |
| Timeline | 指向一个可编辑时间轴版本 |
| Variant | 从现有方案派生 Hook、CTA、比例、语言或受众版本 |
| Render | 设置分辨率、帧率、码率和文件名并执行渲染 |
| Export Set | 管理抖音、小红书、视频号、TikTok 等多规格交付 |

#### 6.3.6 组织与协作节点

- 分组 Frame；
- 便签和标题；
- 评论与批注；
- 审批节点；
- 人工确认节点；
- 子画布或工作流引用。

### 6.4 连线与端口规则

节点连接必须具备类型，避免画布只有视觉连线而没有可执行语义。

建议的基础数据类型：

- `text`
- `brief`
- `script`
- `storyboard`
- `shot`
- `image`
- `video`
- `audio`
- `subtitle`
- `brand`
- `character`
- `product`
- `sequence`
- `timeline`
- `any-media`

规则：

1. 端口只接受兼容类型；
2. 一条边表示数据依赖，不只表示视觉关系；
3. 上游版本变化后，下游显示“可能已过期”，但不自动覆盖已有结果；
4. 用户选择重新运行后，生成新的节点版本；
5. 删除上游节点时，已有输出默认保留，但标记来源缺失；
6. 循环连接默认禁止，工作流执行图保持为 DAG；
7. 评论、便签和纯视觉连线不进入执行图。

### 6.5 节点状态

所有可执行节点统一使用以下状态：

- 草稿 `draft`
- 等待确认 `waiting_confirmation`
- 排队 `queued`
- 运行中 `running`
- 成功 `succeeded`
- 部分成功 `partial_success`
- 失败 `failed`
- 已取消 `cancelled`
- 上游已变化 `stale`

节点卡片至少显示：

- 状态和进度；
- 使用的模型；
- 输入数量和输出数量；
- 预计或实际点数；
- 运行耗时；
- 失败原因和重试入口；
- 当前版本和历史版本。

### 6.6 自动排布与复杂度控制

Agent 自动生成工作流时必须遵守可读性规则：

1. 默认从左到右排列；
2. 同一阶段放入一个 Frame；
3. 一个分镜脚本可以内部管理多个 Shot，不默认把几十个镜头全部平铺；
4. 用户展开某个 Shot 时，才展示其分镜图、视频和声音分支；
5. 已完成且稳定的分组可折叠；
6. 只显示主版本，备选结果放在节点版本托盘中；
7. 提供“整理画布”和“聚焦当前镜头”能力。

---

## 7. AI Director Agent

### 7.1 Agent 定位

Agent 不是一个独立聊天机器人，而是画布的自然语言操作层和视频制片助手。

它负责：

- 读取 Brief、文档、品牌包和参考素材；
- 发现缺失信息并提出必要问题；
- 生成创意策略、脚本和分镜；
- 在画布中创建、连接和整理节点；
- 为不同镜头选择合适模型和参数；
- 发起生成任务并处理部分失败；
- 根据用户反馈修改指定节点，而不是重做整个项目；
- 将确认后的镜头编译为时间轴；
- 解释成本、风险、错误和版本差异。

### 7.2 三种执行模式

#### 建议模式

Agent 只分析和给方案，不创建生成任务。适合早期构思。

#### 确认模式

Agent 可以修改画布结构，但每次产生外部模型费用前必须展示确认卡片。此模式作为默认模式。

确认卡片应展示：

- 本次将运行哪些节点；
- 模型、参数、数量和参考素材；
- 预计时长和点数范围；
- 会生成新版本还是替换当前选择；
- 运行失败时的重试策略。

#### 自动模式

对已经验证的工作流，Agent 可在预算、并发和次数限制内连续执行。

用户必须能够设置：

- 单次任务最大点数；
- 项目最大预算；
- 最大生成次数；
- 允许使用的模型；
- 哪些步骤必须人工确认。

### 7.3 Agent 操作必须可撤销

Agent 对画布的结构性修改应先形成变更计划，然后作为一个可撤销事务提交。生成出来的媒体不物理删除，只从当前画布版本中移除，避免误操作造成资产损失。

---

## 8. 分镜工作流

### 8.1 分镜是核心中间层

ReelMind 不应让 LLM 从一句 Prompt 直接输出最终 Timeline。中间必须存在结构化分镜，因为它承担：

- 用户确认创意和叙事；
- 控制角色、商品和风格一致性；
- 单镜头生成和重试；
- 视频、配音、字幕与音乐对齐；
- 从创作意图到时间轴数据的稳定转换；
- 批量变体和版本比较。

### 8.2 分镜脚本结构

一个 Shot 至少包含：

| 字段 | 含义 |
|---|---|
| 名称与序号 | 镜头身份与顺序 |
| 叙事目的 | Hook、展示卖点、证明、转场或 CTA |
| 画面描述 | 主体、动作、场景、构图、光线和风格 |
| 摄影设计 | 景别、机位、镜头运动、焦段倾向 |
| 台词与旁白 | 该镜头对应文本 |
| 屏幕字幕 | 可与旁白不同 |
| 音乐与音效 | 情绪、节奏、动作声音 |
| 目标时长 | 时间轴占用时长 |
| 输入参考 | 商品、角色、品牌、首尾帧或参考视频 |
| 一致性要求 | 必须保持和允许变化的内容 |
| 生成策略 | 模型、参数、生成数量和选择结果 |
| 当前选中资产 | 最终进入 Sequence 的图、视频和声音 |

### 8.3 单镜头操作

用户可以：

- 修改镜头描述后重新生成；
- 保留构图只改变人物、背景或动作；
- 锁定商品、角色、风格或镜头运动；
- 比较多个候选结果；
- 指定首帧和尾帧生成视频；
- 从上一镜头尾帧派生下一镜头首帧；
- 调整时长、顺序和叙事目的；
- 将候选结果标记为采用、备选或淘汰；
- 只同步当前镜头到时间轴。

### 8.4 分镜视图

除自由画布外，应提供“分镜聚焦视图”：按顺序横向展示所有 Shot，减少用户在大型画布中操作的认知成本。

画布和分镜视图编辑的是同一份数据，不是两个副本。

---

## 9. 画布与时间轴协同

### 9.1 职责边界

| 画布 | 时间轴 |
|---|---|
| 创意资料和上下文 | 帧级播放结构 |
| 脚本与分镜 | Clip、Track 和时间位置 |
| 媒体生成与候选版本 | 裁剪、分割、叠加和变速 |
| 节点依赖和来源追溯 | 字幕样式、转场和音频混合 |
| 工作流模板和批量派生 | 最终预览、渲染和导出 |

### 9.2 从画布生成时间轴

Sequence 节点负责将确认后的分镜编译成 Timeline：

1. 按 Shot 顺序建立主视频轨；
2. 放置每个 Shot 选中的图片或视频；
3. 根据 Shot 时长设置 Clip 长度；
4. 旁白进入语音轨；
5. 音乐和音效进入独立音轨；
6. 字幕进入文本轨；
7. 写入来源关系：Timeline Clip 对应哪个 Shot 和 Asset；
8. 生成一个新的 TimelineVersion，不直接覆盖已有成片。

### 9.3 同步策略

禁止画布与时间轴无提示地双向自动覆盖。

采用以下规则：

- 首次编译创建时间轴版本；
- 上游 Shot 变化后，时间轴对应 Clip 显示“有新版本”；
- 用户选择“替换素材”“保留时长替换”或“完整重新编译”；
- 时间轴上的裁剪、位置、转场和视觉调整默认属于下游覆盖，不反写脚本；
- 如果用户在时间轴调整了镜头顺序，可以选择“同步顺序到 Sequence”；
- 每次批量同步前展示变更摘要；
- 已手工修改的 Clip 必须有保护标记，重新编译时默认不覆盖。

### 9.4 时间轴回到画布

用户可以从时间轴中的 Clip 定位到：

- 来源 Shot；
- 原始生成节点；
- 使用的模型和参数；
- 输入参考；
- 候选和历史版本。

这条反向溯源能力是 ReelMind 相比普通剪辑器的重要优势。

---

## 10. 关键端到端用户流程

### 10.1 从 Brief 创建广告

1. 用户选择“商品广告”模板；
2. 上传商品图、Logo、商品资料和参考视频；
3. AI 提取卖点、限制条件和缺失信息；
4. 生成 3 个创意方向；
5. 用户选择一个方向并编辑脚本；
6. AI 创建结构化分镜脚本；
7. 用户逐镜确认或批量确认；
8. AI 生成分镜图；
9. 用户锁定满意的图片并生成分镜视频；
10. 生成配音、字幕、音乐和音效；
11. Sequence 检查素材完整性；
12. 编译成时间轴；
13. 用户精剪并导出；
14. 从成片派生不同 Hook、CTA 和比例版本。

### 10.2 参考视频复刻

1. 上传参考视频；
2. 执行拉片分析；
3. 生成镜头表、节奏、运镜和声音结构；
4. 用户指定只参考结构、风格或运镜，避免直接复制内容；
5. 将商品和品牌信息映射到新的分镜脚本；
6. 生成新分镜图和视频；
7. 编译时间轴并精修。

### 10.3 批量广告变体

1. 选择一个已完成的 Sequence 或 TimelineVersion；
2. 选择变体维度：Hook、卖点顺序、CTA、受众、时长、比例或语言；
3. 展示变体矩阵和预计成本；
4. 批量生成受影响的节点，复用未变化节点；
5. 创建多个时间轴版本；
6. 批量渲染并按平台命名交付。

---

## 11. 版本与资产管理

### 11.1 版本原则

- 每次生成都创建不可变结果版本；
- 节点的“当前结果”只是对某个版本的引用；
- 重新运行不覆盖旧结果；
- 用户可以比较、恢复和分支；
- 画布定期创建快照；
- 时间轴使用独立 TimelineVersion；
- 成片记录所依赖的画布快照和资产版本。

### 11.2 资产层级

资产分为：

1. 用户级资产：个人长期复用；
2. 团队级资产：品牌包、模板和共享素材；
3. 项目级资产：当前项目上传或生成；
4. 节点输出资产：保留生成参数和来源关系；
5. 交付资产：渲染成片和平台版本。

### 11.3 一致性锁

用户可以对参考内容设置：

- 商品锁；
- Logo/文字锁；
- 角色身份锁；
- 服装锁；
- 场景锁；
- 视觉风格锁；
- 色彩锁；
- 构图锁；
- 运镜锁。

锁定信息是生成任务的结构化参数，不能只拼接成提示词。

---

## 12. 模型、任务与成本体系

### 12.1 模型路由

产品不应要求普通用户理解所有模型。默认由系统根据任务选择：

- 任务类型；
- 商品或角色一致性要求；
- 输入类型；
- 时长和分辨率；
- 速度、质量和预算偏好；
- 模型实时可用性和失败率。

高级用户可以手动指定模型和参数。

### 12.2 任务体系

所有 AI 和渲染任务必须持久化，至少记录：

- 用户、团队、项目和画布；
- 节点、节点版本和运行批次；
- Provider、模型和参数；
- 输入资产和输出资产；
- 状态、进度、重试次数和错误；
- 预计成本、实际成本和用户点数；
- 提交、开始和完成时间；
- 幂等键和取消状态。

任务不能只存在于进程内存中。

### 12.3 费用确认

每次付费生成前展示：

- 预计消耗区间；
- 生成数量；
- 是否启用自动重试；
- 失败是否退还点数；
- 当前项目预算剩余；
- 执行后预计可得到的输出。

系统需要区分：模型成本、平台点数、赠送点数和退款点数。

---

## 13. 当前能力与目标差距

| 领域 | 当前状态 | 目标状态 |
|---|---|---|
| 视频编辑 | 已有多轨时间轴、预览和片段编辑 | 保持并增强帧级精修能力 |
| 渲染 | 已有 BullMQ + FFmpeg 导出 | 支持画布版本、批量规格和渲染集 |
| 素材 | 项目内上传和 AI 素材 | 用户/团队资产、版本、来源和一致性锁 |
| AI 生图/视频 | 单 Provider、简单参数 | 多 Provider 路由、引用控制、成本和质量数据 |
| AI 混编 | 固定规则生成 Timeline | 内容理解、结构化分镜、Sequence 编译 |
| Agent | 队列骨架，处理逻辑未实现 | 可规划、调用工具、操作画布和处理失败的 Director Agent |
| 画布 | 未实现 | 无限画布、类型化节点、DAG 执行、分组和版本 |
| 分镜 | 未实现 | 结构化脚本、Shot、候选和逐镜生成 |
| 任务 | 部分任务持久化，AI 混编仍有内存状态 | 所有生成和渲染任务统一持久化 |
| 商业化 | 未实现 | 点数、订阅、额度、账单和成本控制 |
| 协作 | 未实现 | 分享、评论、审批、团队资产与权限 |

---

## 14. 产品路线图

以下周期按 1～2 名全职研发估算。若团队规模变化，应优先保持里程碑目标，不机械坚持时间。

### Phase 0：画布与任务底座（4～6 周）

目标：建立可持续扩展的画布数据结构和任务系统。

交付：

- Project 增加 Canvas；
- 无限画布基础交互：缩放、拖动、选择、连线、删除、复制、分组；
- 第一批节点：Brief、文本、图片、视频、脚本、分镜脚本、图片生成、视频生成；
- 类型化端口和 DAG 校验；
- 画布自动保存和撤销重做；
- AI Job 持久化，替换 AI 混编内存任务；
- 节点运行状态、进度和失败重试；
- AssetVersion 和 NodeRun 基础模型；
- 生成前参数与成本确认卡片；
- 现有素材生成能力可以从画布节点触发。

验收标准：

- 用户可以手工搭建“文本 → 图片生成 → 视频生成”工作流；
- 刷新页面后画布、任务和结果不丢失；
- 上游变化能正确标记下游过期；
- 单节点失败不会破坏整个画布。

### Phase 1：脚本到分镜图 MVP（6～8 周）

目标：跑通 AI Director 和结构化分镜。

交付：

- 商品广告 Brief 模板；
- 文档、商品、品牌包和参考集合节点；
- Director Agent 基础对话与 `@节点` 引用；
- 创意方向生成；
- 结构化脚本和分镜脚本；
- 分镜聚焦视图；
- Shot 编辑、排序、复制、锁定和批量确认；
- 逐镜分镜图生成；
- 候选结果比较与采用；
- 画布自动布局和分组折叠。

验收标准：

- 用户从商品资料到第一版完整分镜图不超过 10 分钟；
- 每个 Shot 都能独立修改和重新生成；
- 商品、Logo 和风格参考能明确传递到每个生成任务；
- 不需要用户手工复制多轮 Prompt。

### Phase 2：分镜视频到时间轴闭环（6～10 周）

目标：形成可交付的端到端视频生产链路。

交付：

- 首帧、尾帧和参考图生视频；
- 分镜视频候选与版本比较；
- TTS 配音；
- 自动字幕与时间码；
- 音乐和音效生成或推荐；
- Sequence 节点；
- Sequence → Timeline 编译器；
- Shot、Asset、Clip 来源映射；
- 时间轴上游更新提示和选择性同步；
- 成片版本与画布快照关联；
- 9:16 商品广告模板完整闭环。

验收标准：

- 用户可以从 Brief 生成一条可编辑、可播放、可导出的成片；
- 单镜头替换不破坏其他镜头和手工时间轴调整；
- 画布中的任意成片素材可反向定位其来源；
- 项目到成功导出的核心漏斗可完整观测。

### Phase 3：质量、拉片与批量变体（8～12 周）

目标：从“能出片”提升到“稳定生产广告素材”。

交付：

- 多模型 Provider 和模型路由；
- 参考视频拉片；
- 商品与角色一致性检查；
- 图片和视频局部修改；
- Hook、CTA、卖点、比例、语言批量变体；
- 多时间轴版本和批量渲染；
- 平台交付预设；
- 模型质量、耗时、失败率和成本看板；
- 工作流保存为个人模板。

验收标准：

- 一个确认方案可以稳定派生多个广告版本；
- 未变化镜头和资产得到复用，避免重复消耗；
- 模型故障时能够切换或降级；
- 单条成功成片成本可准确核算。

### Phase 4：商业化与团队（8～12 周）

目标：支持稳定收费和小团队协作。

交付：

- 点数钱包和账单流水；
- 免费版、Creator、Studio 和 Team 订阅；
- 并发、队列优先级和预算控制；
- 团队空间、共享资产和品牌包；
- 画布分享、评论和审批；
- 成员角色和额度；
- 团队模板；
- 商业使用说明、内容安全和必要的生成标识信息。

### Phase 5：模板社区与生态（后续）

目标：形成可学习、可复用和可传播的创作网络。

交付：

- 公开作品和创作过程；
- 一键复制工作流；
- 工作流 Remix；
- 模板作者主页；
- 模板收藏、使用和反馈；
- 第三方模型或 App 接入规范；
- 模板分成和生态激励。

只有当核心生产闭环和留存成立后才进入此阶段。

---

## 15. 版本优先级

### P0：闭环必需

- 无限画布基础能力；
- 类型化节点和连线；
- AI 任务持久化；
- Brief、脚本、分镜脚本和 Shot；
- 分镜图、分镜视频；
- Sequence → Timeline；
- 单镜头版本与重新生成；
- 成本确认、失败重试和来源追溯。

### P1：形成竞争力

- Director Agent；
- 商品、品牌和角色一致性；
- TTS、字幕、音乐和音效；
- 参考视频拉片；
- 多模型路由；
- 批量变体；
- 工作流模板。

### P2：规模化

- 团队空间与审批；
- 订阅与点数；
- 社区与 Remix；
- App 生态；
- 企业权限与合规能力。

---

## 16. MVP 明确不做

首个无限画布版本不做：

- 多人实时光标和实时共同编辑；
- 第三方插件市场；
- 用户自定义代码节点；
- 任意循环和复杂条件分支；
- 广告投放与投后数据归因；
- 市场洞察和电商站点运营；
- 长篇影视项目管理；
- 移动端完整画布编辑；
- 数十个模型的无差别接入；
- 公开模板交易市场。

这些能力会显著扩大复杂度，但无法验证最核心的“画布到可编辑成片”价值。

---

## 17. 商业模式

建议采用“订阅权益 + 点数消耗 + 团队席位”的组合。

### Free

- 新用户体验点数；
- 有限项目数和并发；
- 可跑通一条低成本工作流；
- 导出规格或水印存在限制。

### Creator

- 适合个人创作者；
- 月度点数；
- 商业使用和高清导出；
- 个人模板和品牌包；
- 标准生成队列。

### Studio

- 更高点数和并发；
- 优先队列；
- 批量广告变体；
- 多品牌包；
- 批量渲染和平台交付集。

### Team

- 共享点数池；
- 成员与额度管理；
- 团队资产和模板；
- 评论、审批和项目权限；
- 账单与使用报表。

定价不直接照抄竞品。上线付费前，需要先获得以下真实数据：

- 各模型单次生成成本；
- 失败和自动重试成本；
- 单个 Shot 平均候选数量；
- 单条成功成片平均成本；
- 用户愿意为速度、质量和并发支付的溢价。

早期综合毛利目标不低于 40%，模型路由和规模稳定后向 60% 提升。

---

## 18. 产品指标

### 18.1 北极星指标

> 每周成功导出的 AI 辅助可编辑成片数量。

必须同时满足“AI 辅助”“进入可编辑时间轴”和“成功导出”，避免用生成次数制造虚假增长。

### 18.2 激活指标

- 首次进入产品到创建第一个有效画布；
- 首次生成完整分镜脚本；
- 首次生成全部分镜图；
- 首次生成可播放 Sequence；
- 首次成功导出；
- 首次成片时间，目标小于 10 分钟。

### 18.3 核心漏斗

```text
创建项目
  → 完成 Brief
  → 选择创意方向
  → 确认分镜
  → 生成完整素材
  → 编译时间轴
  → 成功导出
  → 创建第二个版本或第二个项目
```

### 18.4 质量与效率指标

- 节点生成成功率；
- 单 Shot 平均重生成次数；
- 候选采用率；
- 分镜到时间轴编译成功率；
- 时间轴手工替换镜头比例；
- 平均出片时间；
- 单条成功成片成本；
- 批量变体复用率；
- 导出失败率；
- 用户主动修改脚本和分镜的比例。

### 18.5 留存与商业指标

- 次日和 7 日创作留存；
- 7 日内第二次项目创建率；
- 每周导出用户数；
- 免费到付费转化；
- 点数消耗与充值率；
- 订阅续费率；
- 毛利率；
- 团队项目中的协作人数和审批次数。

---

## 19. 核心风险与应对

### 19.1 画布过度复杂

风险：用户需要学习节点、端口、模型和参数，创作反而变慢。

应对：

- 默认由 Agent 和模板创建工作流；
- 普通用户只需编辑内容和选择结果；
- 高级参数渐进披露；
- 提供分镜聚焦视图；
- 自动布局、折叠和版本托盘减少节点数量。

### 19.2 多模型成本失控

风险：自动工作流连续生成导致平台亏损或用户误消费。

应对：

- 默认确认模式；
- 项目预算和单任务预算；
- 运行前成本预估；
- 复用未变化节点；
- 失败退款和重试规则透明；
- 模型成本、失败率和质量持续观测。

### 19.3 一致性不足

风险：商品、人物和风格在不同镜头间漂移，成片不可用。

应对：

- 结构化一致性锁；
- 每个 Shot 显式引用商品、角色和品牌资产；
- 先分镜图确认，再生视频；
- 自动一致性检查；
- 支持锁定首尾帧和从上一镜头派生。

### 19.4 画布与时间轴冲突

风险：用户不知道哪边是最新版本，重新生成覆盖手工编辑。

应对：

- 显式编译和同步；
- 上游变化只提示，不自动覆盖；
- 手工修改保护；
- 同步前展示 Diff；
- TimelineVersion 与 CanvasSnapshot 绑定。

### 19.5 技术与授权风险

风险：画布组件、媒体处理库或模型接口存在商用授权、稳定性和供应商锁定问题。

应对：

- 延续项目现有的商用授权审查原则；
- 画布库选型前验证许可证和团队规模限制；
- Provider 层隔离模型厂商；
- 保留输入、输出和参数，支持任务迁移与重新执行；
- 关键媒体资产落自有对象存储。

---

## 20. 建议的数据领域对象

后续技术设计可以围绕以下领域对象展开：

```text
Workspace
User / Membership
Project
Canvas
CanvasNode
CanvasEdge
CanvasSnapshot
NodeVersion
NodeRun
GenerationJob
ModelProvider / ModelDefinition
Asset
AssetVersion
Brief
BrandKit
ProductProfile
CharacterProfile
Script
Storyboard
Shot
Sequence
TimelineVersion
RenderJob
ExportSet
CreditWallet / CreditLedger
Comment / Approval
WorkflowTemplate
```

核心关系：

```text
Project
 ├─ Canvas ─ CanvasSnapshot
 │    ├─ CanvasNode ─ NodeVersion ─ NodeRun
 │    └─ CanvasEdge
 ├─ Storyboard ─ Shot ─ AssetVersion
 ├─ Sequence ─ TimelineVersion ─ RenderJob
 └─ Asset ─ AssetVersion
```

具体数据库设计需在 Phase 0 单独制定，不建议继续把所有新状态塞进 `Project.timeline` JSON。

---

## 21. 产品决策原则

后续遇到功能取舍时，按以下顺序判断：

1. 是否缩短从 Brief 到可编辑成片的时间；
2. 是否提升商品、角色或品牌的一致性；
3. 是否让用户能局部调整，而不是整条重做；
4. 是否让生成过程可追溯、可复用；
5. 是否降低单条成功成片的成本；
6. 是否改善批量广告变体效率；
7. 是否能被真实留存和付费数据验证。

如果一项功能只是让画布看起来更强大，却不能改善上述目标，应降低优先级。

---

## 22. 下一步行动

进入开发前，依次完成：

1. 为 Phase 0 编写独立 PRD；
2. 绘制画布主界面、节点卡片、右侧属性面板和任务栏交互稿；
3. 评估画布渲染与图编辑技术选型及许可证；
4. 设计 Canvas、Node、Edge、NodeRun、AssetVersion 数据模型；
5. 定义第一批节点的输入输出契约；
6. 统一 AI Job、RenderJob 和成本记录；
7. 制定画布到现有 Timeline Schema 的编译协议；
8. 用“商品广告”模板完成第一次端到端可用性测试。

第一阶段不是一次做完整个 Creative OS，而是验证一个关键事实：

> 用户是否愿意在 ReelMind 的画布中完成脚本和分镜，并把结果继续带入时间轴完成真实交付。
