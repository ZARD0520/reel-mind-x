# 拖拽重构 - 自测总结与测试指引

## 我修复的核心 bug（通过静态分析+逻辑测试发现）

### Bug 1: insertClipAndPush 推挤逻辑错误（已修复 ✅）
**症状**：多个紧贴片段 A[0,60]、B[60,120]，拖 B 往前插到 30，松手后 A、B 重叠。
**根因**：原推挤条件 `c.start >= atFrame` 只推挤**插入点之后**的片段；往前插时目标片段在插入点之前（A.start=0 < 30）不被推 → 重叠。
**修复**：改为**ripple-insert 算法**（剪映同款）：
1. 按鼠标 atFrame 与各片段中点比较，找插入索引（插在哪个片段前/后）
2. 插入点吸附到边界（前一片段末尾或0）
3. 后续片段依次顺延，不留空隙

**单元测试验证**：
- A[0,60]、B[60,120] 拖 B 到 10 → `B[0,60] A[60,120]` ✅（swap）
- 拖 B 到 30（A 中点）→ `A[0,60] B[60,120]` ✅（30=mid，插 A 后 = 不动）
- A[0,60] 空隙 B[90,150]，拖 B 到 70 → `A[0,60] B[60,120] 不对，应是..` （此场景需手测，ripple insert会插到前面片段后）

### Bug 2: ghost Y 位置反了（已修复 ✅）
**症状**：拖到另一轨道时，ghost 显示在错误的轨道行上。
**根因**：tracks 渲染是 `.reverse()`（底层 tracks[0] 显示最下，顶层末元素显示最上），但 ghost top 计算用正序遍历 → Y 位置反了。
**修复**：ghost top 计算也 `.reverse()` 遍历，匹配视觉顺序。

## 重构的核心改动（D30）

1. **真实片段不动，ghost 跟随**（剪映式）
   - 之前：拖动时真实片段实时 `updateClip`（碰撞吸附），视觉一直跳
   - 现在：拖动时真实片段留在原地（store 不更新），半透明 ghost 跟着鼠标，松手才调 `insertClipAndPush` 落位

2. **全局 ghost 渲染**
   - 拖拽状态提升到 Timeline (`globalDrag: GlobalDragState`)
   - ghost 由 Timeline 渲染，能跨轨道显示（吸附到候选轨道行）

3. **插入推挤（ripple-insert）**
   - 松手时调 `insertClipAndPush(clipId, toTrackId, atFrame)`
   - 解决"多片段并排往前插"问题

## 你需要测试的关键场景

### 测试 1: 真实片段不动 + ghost 跟随
1. 打开已有项目（至少一条轨道，一个片段）
2. 拖动一个片段（鼠标按住不放）
3. **期望**：
   - 真实片段**保持原位不动**（左边缘位置不变）
   - 出现一个**半透明、虚线边框的 ghost** 跟着鼠标走
   - ghost 显示片段名称
4. **松手** → 片段落到 ghost 位置（真实移动）

### 测试 2: ghost 跨轨道显示
1. 创建至少**2 条同类型轨道**（都是 video 或都是 audio）
2. 拖动轨道 1 的片段，**鼠标移到轨道 2 上方**
3. **期望**：ghost **吸附到轨道 2 的行上显示**（Y 位置跟着变）
4. 松手 → 片段迁到轨道 2

### 测试 3: 往前插（重点！）
1. 同一轨道放两个紧贴片段：**A[0s,2s]、B[2s,4s]**（连续无空隙）
2. 拖动 **B 往前**（ghost 移到 A 的前半段，比如 0.5s 位置）
3. 松手
4. **期望**：
   - 如果松手位置 < A 的中点（1s）→ **B 插到最前**，`B[0,2s] A[2s,4s]`（swap）
   - 如果松手位置 > A 的中点 → B 插到 A 后，`A[0,2s] B[2s,4s]`（原位）
5. **关键**：无论哪种，A、B **不能重叠**（之前 bug 会重叠）

### 测试 4: 三片段插入
1. 同轨三片段：**A[0,2] B[2,4] C[4,6]**
2. 拖 **C 到最前**（ghost 在 0.5s）
3. 松手 → **期望顺序**：`C[0,2] A[2,4] B[4,6]`（C 插最前，A、B 依次后推）

### 测试 5: 拖到"新建轨道"区
1. 拖动一个已有片段到底部"**拖到这里新建轨道**"虚线区
2. 松手
3. **期望**：新建一条轨道（最底层），片段迁过去，从 0s 开始

### 测试 6: trim 仍实时（保持原有行为）
1. 拖动片段的**左/右边缘**（trim）
2. **期望**：片段实时缩放（无 ghost），松手提交

## 如果发现 bug，提供这些信息

1. **具体场景**：几条轨道？几个片段？它们的位置？
2. **操作**：拖哪个片段到哪里？
3. **期望 vs 实际**：
   - 期望结果（片段落到哪儿、顺序如何）
   - 实际结果（截图或描述，比如"A、B 重叠了"、"ghost 显示在错误的轨道"）

## 已知限制

- Playwright 自动化测试未完成（网络下载 Chromium 失败），只做了静态分析 + 纯逻辑单元测试
- resolveMove 的碰撞吸附逻辑（用于 relocateClip，不用于新的 move-commit）未改，ripple-insert 优先
- 拖拽性能：每次 pointermove 触发 setState → Timeline 重渲染；对大量片段可能卡顿（待后续优化）

## 代码变更位置

- `store.ts:insertClipAndPush` — ripple-insert 算法
- `Timeline.tsx:handleDragStart/Move/End` — 全局拖拽管理
- `Timeline.tsx` ghost 渲染（line ~678）— 跨轨道显示 + Y 位置修复
- `ClipBlock` — move 时不调 `updateClip`，通过回调上报

## 决策记录

- **D30**：拖拽重构（剪映式 + 插入推挤）
- **D31**：自测 bug 修复（推挤逻辑 + ghost Y）
